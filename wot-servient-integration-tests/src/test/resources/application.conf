relay {
  monitoring {
    enabled = false
  }
}

wot {
  servient {
    akka {
      client.akka.p2p.relay.host = "localhost"
      server.akka.p2p.relay.host = "localhost"
    }

    mqtt {
      broker = "tcp://eclipse-mosquitto:1883" # change to "localhost" to run tests locally
      # username = "myusername"
      # password = "mysecretpassword"
    }
  }
}