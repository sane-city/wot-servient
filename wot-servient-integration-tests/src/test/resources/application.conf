wot {
  servient {
    mqtt {
      broker = "tcp://eclipse-mosquitto:1883" # change to "localhost" to run tests locally
      # username = "myusername"
      # password = "mysecretpassword"
    }
  }
}